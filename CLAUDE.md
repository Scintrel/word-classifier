# 单词分类app (Word Classification App)

## 项目概述

一个跨平台桌面应用，用于导入、管理、分类和导出英语单词表。
支持 CSV、Excel、TXT、JSON 等文件格式，能够将单词、音标、释义、例句等多维度信息关联在一起，
自动检查并补全错误数据，并按语义自动将单词分类到不同单元。

**目标平台**: Windows + Mac

---

## 技术栈

| 层级 | 技术 | 说明 |
|------|------|------|
| 桌面框架 | Electron 33+ | 把网页技术打包成桌面应用 |
| 前端界面 | React 18 + TypeScript | 构建用户界面的组件化框架 |
| 状态管理 | Zustand | 轻量级状态管理，比 Redux 简单 |
| 样式 | Tailwind CSS + shadcn/ui | 原子化CSS + 可复用UI组件 |
| 数据库 | sql.js | 纯 JavaScript 的 SQLite，无需编译，跨平台零问题 |
| 文件解析 | xlsx, papaparse | 解析 Excel 和 CSV 文件 |
| 打包 | electron-builder | 打包成 .exe (Windows) / .dmg (Mac) |

---

## ⚠️ 重要：如何与我（用户）协作

**我是完全的小白用户。** 我不懂 "IPC"、"ORM"、"migration"、"middleware"、"依赖注入" 等术语。

### 你必须遵守的规则：

1. **永远先给方案，让我选择。** 每个技术决策，列出 2-3 个选项，用大白话解释每个选项的优缺点（对我作为用户意味着什么——速度快慢？是否稳定？好不好用？），然后让我决定。

2. **用大白话解释。** 
   - ❌ 不要说："我们需要实现一个带 contextBridge 隔离的 IPC handler"
   - ✅ 要说："我们需要建立一条通信通道，让界面（你看到的部分）能和后台（处理文件和数据的地方）对话。有2种方式..."

3. **协作流程永远是：**
   - 你列出方案（用大白话 + 优缺点）
   - 我选择用哪个方案
   - 你来实现

4. **不要假设我懂任何技术概念。** 遇到可能不熟悉的概念，用1-2句大白话先解释，再让我决定。

5. **当我问"为什么"时，** 用我能感受到的结果来解释："这样做，软件打开会更快" 或 "这样做，即使你有10000个单词，列表也能秒开。"

6. **当出错时，** 先用大白话解释发生了什么，然后提出修复方案。

---

## 项目架构

### 两个进程

这是一个 Electron 应用，有两个"进程"（可以理解为两个同时运行的部分）：

- **主进程 (Main Process)** (`src/main/`)：在后台运行，负责读写文件、操作数据库、处理数据。相当于"引擎室"。
- **渲染进程 (Renderer Process)** (`src/renderer/`)：就是你能看到的界面（用 React 写的）。显示所有内容，处理你的点击和输入。相当于"仪表盘"。

两个进程通过 IPC（进程间通信）来对话——主进程和渲染进程之间有一条安全的通道。

### 数据流（一个单词文件导入后经历了什么）

```
你拖入文件
    ↓
文件解析器读取文件 → 识别格式(CSV/Excel/TXT) → 提取数据
    ↓
列映射界面 → 你告诉程序"这列是单词，那列是音标..."
    ↓
数据校验 → 检查缺失、重复、格式错误
    ↓
自动分类 → 按语义把单词分组（食物、科技、情感...）
    ↓
你可以浏览、编辑、搜索、导出
```

### 核心模块

| 模块 | 位置 | 作用 |
|------|------|------|
| 文件解析器 | `src/main/parser/` | 读取上传的文件，提取单词数据 |
| 数据库层 | `src/main/database/` | 存储和查询所有单词、分类、例句 |
| 数据校验 | `src/main/validation/` | 检查导入的数据有没有错误，自动补全缺失信息 |
| 语义分类 | `src/main/classification/` | 自动把单词按意思分组 |
| IPC 处理器 | `src/main/ipc/` | 界面和后端的通信桥梁 |
| UI 组件 | `src/renderer/components/` | 可复用的界面元素（表格、按钮、拖拽区域） |
| 页面 | `src/renderer/views/` | 完整页面（导入页、单词列表、分类页等） |
| 状态存储 | `src/renderer/stores/` | 记录界面当前显示什么数据 |

---

## 数据库结构（大白话版）

- **words（单词表）**: 每行=一个单词，包含拼写、音标、释义、词性、难度等
- **examples（例句表）**: 每个单词可以有多个例句，每个例句可以有中文翻译
- **categories（分类表）**: 主题分组，如"食物"、"科技"、"自然"，可以有多级子分类
- **word_categories（单词-分类关联表）**: 记录每个单词属于哪个分类（一个单词可以属于多个分类）
- **word_relations（单词关系表）**: 单词之间的关系（近义词、反义词等）
- **import_history（导入历史）**: 记录每次导入的文件信息
- **validation_log（校验日志）**: 记录发现和修复的数据问题
- **settings（设置）**: 应用的偏好设置

---

## 项目目录结构

```
单词分类app/
├── package.json                 # 项目配置（依赖包、脚本命令）
├── tsconfig.json                # TypeScript 配置
├── electron-builder.yml         # 打包配置（生成 .exe / .dmg）
├── vite.config.ts               # 构建配置
├── CLAUDE.md                    # ← 你正在看的文件
│
├── src/
│   ├── main/                    # 主进程（后台引擎）
│   │   ├── index.ts             # 入口：创建窗口、注册IPC
│   │   ├── ipc/                 # IPC 通信处理器
│   │   ├── parser/              # 文件解析
│   │   ├── database/            # 数据库操作
│   │   ├── validation/          # 数据校验和自动补全
│   │   └── classification/      # 语义分类引擎
│   │
│   ├── preload/                 # 预加载脚本（安全桥梁）
│   │
│   └── renderer/                # 渲染进程（用户界面）
│       ├── index.html           # HTML 入口
│       ├── main.tsx             # React 入口
│       ├── App.tsx              # 根组件 + 路由
│       ├── components/          # 可复用组件
│       │   ├── ui/              # 基础UI（按钮、输入框、弹窗等）
│       │   ├── AppShell.tsx     # 主布局框架
│       │   ├── Sidebar.tsx      # 侧边导航栏
│       │   ├── FileDropZone.tsx # 文件拖拽上传区域
│       │   ├── WordTable.tsx    # 单词表格
│       │   └── CategoryTree.tsx # 分类树
│       ├── views/               # 页面
│       │   ├── ImportView.tsx   # 导入页面
│       │   ├── WordListView.tsx # 单词列表页
│       │   ├── CategoryView.tsx # 分类浏览页
│       │   ├── SearchView.tsx   # 搜索页
│       │   ├── DataEditorView.tsx # 数据编辑页
│       │   └── SettingsView.tsx # 设置页
│       ├── stores/              # 状态管理
│       ├── hooks/               # 自定义 React Hooks
│       └── styles/              # 样式文件
│
└── resources/                   # 静态资源
    ├── icon.ico                 # Windows 图标
    └── icon.icns                # Mac 图标
```

---

## 开发命令

```bash
# 安装依赖（第一次运行）
npm install

# 启动开发模式（边改代码边看效果）
npm run dev

# 构建生产版本
npm run build

# 打包为 Windows 安装包
npm run package:win

# 打包为 Mac 安装包
npm run package:mac
```

---

## 设计原则

- **所有数据都在本地。** 不会上传到互联网，你的单词表只在你电脑上。
- **绝不偷偷改数据。** 自动补全和修复都会先让你确认，不会直接修改。
- **支持大量单词。** 即使有 50,000 个单词也能流畅运行。
- **中文友好。** 界面支持中英文，内置分类有中文名称。
- **可以撤销导入。** 每次导入都有记录，可以回退。

---

## 开发路线图

整个项目分 6 个阶段开发，每个阶段完成后需要你确认再进入下一阶段。

### ✅ Phase 1：项目基础搭建（已完成）

- [x] Electron + React + TypeScript 项目初始化
- [x] 数据库连接（sql.js / SQLite）
- [x] 数据库表结构创建（8张表）
- [x] 预置31个语义分类
- [x] 主进程 IPC 通信框架
- [x] 前端 6 个页面框架 + 侧边栏导航
- [x] Tailwind CSS 样式 + 浅色/深色主题
- [x] **构建验证通过**

### ⏳ Phase 2：文件导入与解析（已完成）

**目标**：能上传单词表文件，自动识别格式并导入数据库。

- [x] 实现 CSV 解析器（支持不同分隔符和编码）
- [x] 实现 Excel 解析器（.xlsx / .xls）
- [x] 实现 TXT 解析器（一行一词 或 tab分隔）
- [x] 实现 JSON 解析器
- [x] 文件拖拽上传区域（FileDropZone 组件）
- [x] 文件预览——导入前先显示前20行数据（表格形式）
- [x] 列映射界面——下拉选择，自动匹配列名
- [x] 导入进度 + 导入结果汇总（成功/跳过/警告）
- [x] 导入历史记录

### ✅ Phase 3：单词管理（已完成）

**目标**：能浏览、搜索、编辑已导入的单词。

- [x] 单词列表（排序、分页、50条/页）
- [x] 单词详情面板（侧边滑出，可编辑所有字段）
- [x] 按分类筛选（下拉选择）
- [x] 按难度筛选（初级/中级/高级）
- [x] 单词搜索（搜索单词或释义）
- [x] 编辑单词字段（拼写、音标、释义、词性、难度）
- [x] 例句管理（添加/删除例句）
- [x] 手动分配分类（点选标签）
- [x] 删除单词（带确认）

### ✅ Phase 4：数据校验与自动补全（已完成）

**目标**：自动检测导入数据中的问题，提供一键修复。

- [x] 必填字段检查（单词拼写不能为空）
- [x] 音标格式检查（检测无效音标）
- [x] 编码乱码检测（UTF-8/GB2312 乱码识别）
- [x] 重复单词检测和标记
- [x] 释义语言匹配检查（中文释义不是中文、英文释义不是英文）
- [x] 词性未设置检查
- [x] 音标自动补全（内置 ~150 常用词词典）
- [x] 释义自动补全（从词典查询）
- [x] 词性智能推测（基于后缀规则：-tion→名词、-ly→副词等）
- [x] 问题列表界面（按类型筛选、展开详情）
- [x] 一键自动修复（补全缺失字段）
- [x] 数据完整度统计仪表盘

### ✅ Phase 5：语义分类引擎（已完成）

**目标**：让程序自动把单词按意思分组。

- [x] 关键词匹配分类（10个主分类 + 20+子分类，共600+关键词）
- [x] 基于定义的语义匹配（搜索中文和英文释义中的关键词）
- [x] 分类结果置信度评分（0.3–1.0，基于匹配数量）
- [x] "一键自动分类所有单词"功能
- [x] 手动分类不会被自动分类覆盖
- [x] 分类浏览页（可展开树形结构 + 单词数统计）
- [x] 按分类导出 JSON 文件
- [x] 分类结果反馈（显示匹配关键词和置信度）

### ✅ Phase 6：导出与打包（已完成）

**目标**：把分类好的单词表导出为文件，打包成安装程序。

- [x] 导出 JSON（全部/按分类/按难度）
- [x] 设置页面（导出格式选择、数据管理）
- [x] 侧边栏导航完整可用
- [x] 应用图标和名称
- [x] Windows 打包配置 (.exe)
- [x] Mac 打包配置 (.dmg)
- [x] 构建验证通过（所有6个阶段）

---

## 当前状态

- **所在阶段**：🎉 全部 6 个阶段已完成！
- **可运行命令**：`npm run dev`（开发模式）/ `npm run package:win`（打包 Windows 安装包）
- **项目文件**：28 个源文件 + 7 个配置文件

---

## 常见任务指引

### 添加新的文件格式支持
1. 在 `src/main/parser/` 创建新的解析器文件（如 `xml.parser.ts`）
2. 实现 `IParser` 接口中的 `parse()` 方法
3. 在 `parser.factory.ts` 中注册新格式

### 添加新页面
1. 在 `src/renderer/views/` 创建页面组件
2. 在 `Sidebar.tsx` 添加导航项
3. 在 `App.tsx` 添加路由

### 修改数据库结构
1. 在 `src/main/database/migrations/` 创建新的 SQL 迁移文件
2. 更新 `migrations.ts` 引入新迁移
3. 下次启动应用时自动执行
